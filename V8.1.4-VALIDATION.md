# V8.1.4 — état de validation

## Verdict

**CODE PRÊT POUR VALIDATION NEON.**

## Exécuté localement

- installation reproductible avec `npm ci` ;
- lint ;
- tests unitaires ;
- build Vite ;
- `npm audit --omit=dev` ;
- `npm run validate:release` ;
- contrôle des checksums `0001→0005`.

## À exécuter avec les services réels

- `npm run validate:neon` avec les quatre variables de test ;
- Playwright dans GitHub Actions (téléchargement Chromium indisponible dans l'environnement de construction) ;
- Preview Vercel ;
- production après levée du médiateur.

La CI bloque désormais si les secrets Neon de test manquent. `cimes_app` reste
un rôle `NOLOGIN` et l'URL runtime doit utiliser un rôle Neon `LOGIN` distinct,
non propriétaire, associé par `npm run runtime:grant`.
